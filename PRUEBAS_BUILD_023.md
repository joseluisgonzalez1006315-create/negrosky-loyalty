# Pruebas BUILD 023

- Sintaxis validada en backend y frontend.
- Nombre del negocio usado automáticamente cuando no se escribe un nombre alternativo.
- Eventos `input` y `change` conectados a la vista previa.
- URL del trabajador generada con protocolo, IP y puerto reales del servidor.
- Detección alternativa de IP LAN mediante interfaz de salida.

