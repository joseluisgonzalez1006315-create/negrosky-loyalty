# PRUEBAS · BUILD 040

- Verificación sintáctica de `app/main.py`, `app/database.py` y `app/schemas.py`.
- Verificación sintáctica de `web/app.js`, `web/customer.js` y `web/worker.js`.
- Verificación de que las referencias activas muestran BUILD 040.
- La suite completa queda pendiente de ejecutarse en Windows con el entorno incluido.

