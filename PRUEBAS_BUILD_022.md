# Pruebas BUILD 023

- Verificación sintáctica de `app/main.py` y `app/schemas.py`.
- Verificación sintáctica de `web/customer.js` y `web/app.js`.
- Tarjetas conservan compras, progreso, premios agotados y reclamo múltiple.
- Cálculo visual protegido contra división por cero.

