# PRUEBAS · BUILD 041

- `app/main.py`, `app/database.py` y `app/schemas.py` verificados sintácticamente.
- `web/customer.js`, `web/app.js` y `web/worker.js` verificados sintácticamente.
- Barra rápida limitada a la vista móvil.
- Botones desplazan suavemente al módulo correspondiente.
- No cambia la lógica de operaciones ni la base de datos.

